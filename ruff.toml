line-length = 80

exclude = [
    "settings.py",
    # уточнить, нужен ли у миграций слэш внизу, если это папка
    "migrations" 
]

[lint.per-file-ignores]
# init modules can contain the local imports, logic, unused imports
"__init__.py" = ["F401"]

[lint]
preview = true
select = ["E", "F", "I", "C90"]